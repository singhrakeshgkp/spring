
server:
  port: 8787
spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://mysqldb:3306/graphqltest
    username: sysuser
    password: root
    
    
  graphql:
    graphiql:
      enabled: true
    cors:
      allowed-origins: "*"
      